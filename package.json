{
  "name": "gaato-net",
  "private": true,
  "type": "module",
  "scripts": {
    "patch:luna": "node scripts/patch-luna.mjs",
    "dev": "moon update && bun run patch:luna && vite",
    "build": "moon update && bun run patch:luna && moon build --release && vite build"
  },
  "devDependencies": {
    "vite": "^6.0.0",
    "vite-plugin-moonbit": "^0.1.0"
  }
}
